'use strict';

/**
 * @ngdoc function
 * @name microWorkApp.controller:CreateCtrl
 * @description
 * # CreateCtrl
 * Controller of the microWorkApp
 */
angular.module('microWorkApp')
  .controller('CreateCtrl', function ($scope) {
    $scope.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
